import { Utilisateurresponse } from './utilisateurresponse';

describe('Utilisateurresponse', () => {
  it('should create an instance', () => {
    expect(new Utilisateurresponse()).toBeTruthy();
  });
});
